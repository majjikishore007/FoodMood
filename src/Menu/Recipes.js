import React from 'react';
import Base from '../Core/Base';

const recipes = () => {
  return (
    <div>
      <Base></Base>
      <h1>all resipes dashboard</h1>
    </div>
  );
};

export default recipes;
